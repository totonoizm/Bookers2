 <!--サイドバー部分 aplication.htmlでrender使って呼び出し-->
 <h2>UserInfo</h2>
 <% if user_signed_in? %>
  <%= attachment_image_tag current_user, :profile_image, size: "40x40", format: 'jpeg',fallback: "no_image.jpg" %><!-- !!user.showへリンクしたい！！bookmodelに紐付けたuserのprofile_imageを表示します-->
   <p>introduction <%= current_user.introduction %></p>
   <p><%= link_to " ", edit_user_path(current_user), class: "btn btn-default btn-block glyphicon glyphicon-wrench" %></p>


 <!--通常はログインユーザー情報、usershowの時のみユーザー情報を表示-->
          <div>
            <%= form_with model:book, local:true do |f| %>
              <div>
                <p>Title</p>
                <%= f.text_field :title %>
              </div>
              <div>
                <p>Opinion</p>
                <%= f.text_area :body %>
              </div>
              <div>
                <%= f.submit 'Create Book' %>
              </div>
            <% end %>
          </div>

 <% end %>